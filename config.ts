export const CONFIG = {
  // Wallet found interval (every 7 seconds)
  WALLET_FOUND_INTERVAL_MS: 18000,
  
  // Console logs per second
  LOGS_PER_SECOND: 40,
  
  // Wallet checked increment per second
  CHECK_INCREMENT_PER_SEC: 8000000000,
  
  // Found amount range
  FOUND_MIN: 734.85,
  FOUND_MAX: 10249,
  
  // UI Colors
  NEON_GREEN: '#39FF66',
  BACKGROUND_BLACK: '#0a0a0a',
  CONSOLE_BLACK: '#000000',
  
  // Console buffer size (max logs to keep)
  MAX_LOGS: 100,
};